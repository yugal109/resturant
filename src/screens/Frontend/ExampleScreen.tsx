import * as React from 'react';
import { View } from 'react-native';

import { Text } from '../../ui';

export const ExampleScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text>This is an example frontend screen</Text>
    </View>
  );
};
